.env
# Per-run skill scratch & outputs — never part of a skill itself
skills/*-workspace/
skills/*/*-workspace/
screenshots-output/

# Scheduler lock file and worktree checkouts
scheduled_tasks.lock
worktrees/
